import { COLORS, FONT_SIZE, FONT_WEIGHT } from "../../styles/theme";

export type FONT_SIZE_KEY = keyof typeof FONT_SIZE;
export type FONT_WEIGHT_KEY = keyof typeof FONT_WEIGHT;
export type COLORS_KEY = keyof typeof COLORS;
