import { DefaultTheme } from "styled-components";

const colors = {
  black: "#010101",
  deepGray: "#434241",
  lightGray: "#bababa",
  lightCyan: "#98E9F4",
  white: "#f8f8f8",
  mint: "#8CBEC9",
};

const fontSize = {
  xs: "0.5rem",
  s: "0.75rem",
  m: "1rem",
  l: "1.25rem",
  xl: "1.5rem",
};

const theme: DefaultTheme = {
  colors,
  fontSize,
};

export default theme;
